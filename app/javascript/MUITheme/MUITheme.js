import { createTheme } from '@material-ui/core/styles';

const MUITheme = createTheme({
  palette: {
    primary: {
      main: '#0caa41',
    },
    secondary: {
      main: '#757575',
    },
  },
});

export default MUITheme;
